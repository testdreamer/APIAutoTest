#-- coding: utf-8 --#@Time : 2022/8/23 14:54#@Author : tianxh#@Email : tianxh@casking.com.cn#@File : get_html_data.py#@Software: PyCharmfrom pyquery import *def get_html_data(file):    """    获取html的数据    :param file: html文件    :return:    """    html = open(file, 'r', encoding='utf-8').read()  # local html    querys = pyquery.PyQuery(html.encode('utf-8'))    res = querys("div").eq(4).text().replace('概要 ', '&').replace('错误', '&').replace('失败', '&').replace('成功','&').replace('所有', '&').split('&')    return resdef get_html_by_id_create_list(file):    """    读取html文件的内容,通过id获取文本后生成元祖添加到list中    :param file: html文件路径    :return: 返回一个元祖元素的列表    """    html = open(file, encoding='utf-8').read()  # local html    querys = pyquery.PyQuery(html.encode('utf-8'))    res = querys("tr").items()    sum = 0    for item in res:        if item.attr("class") == 'passClass':            sum += 1    data = []    for i in range(0, sum):        r = querys(".passClass").eq(i).text().split('\n')[0]        data.append((r, '详情'))    data.insert(0, ('测试套件/测试用例', '查看'))    return datadef read_html(file):    """    读取html文件内容    :param file:html文件路径    :return:    """    html = open(file, 'r', encoding='utf-8').read()  # local html    querys = pyquery.PyQuery(html.encode('utf-8'))    return  querys